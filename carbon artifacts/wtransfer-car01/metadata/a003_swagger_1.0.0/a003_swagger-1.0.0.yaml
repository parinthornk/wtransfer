openapi: 3.0.1
info:
  title: a003
  description: API Definition of a003
  version: 1.0.0
servers:
- url: http://localhost:8290/wtransfer
paths:
  /:
    get:
      responses:
        default:
          description: Default response
    post:
      responses:
        default:
          description: Default response
    delete:
      responses:
        default:
          description: Default response
    patch:
      responses:
        default:
          description: Default response
